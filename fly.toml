app = 'rprxnext'
# swap_size_mb = 2048

[build]

[deploy]
  strategy = "immediate"
  max_unavailable = 1

[http_service]
internal_port = 80
force_https = true
auto_stop_machines = 'off'
auto_start_machines = true
min_machines_running = 1
processes = ['app']
